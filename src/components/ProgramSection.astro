<section class="program-section">
  <div class="container">
    <p class="program-label">ПРОГРАММА МЕРОПРИЯТИЯ</p>
    <h2 class="program-title">ЧТО ВАС ЖДЁТ</h2>
    <p class="program-description">
      ТРИ ДНЯ НАСЫЩЕННОЙ ПРОГРАММЫ: МАСТЕР-КЛАССЫ ОТ ПРОФЕССИОНАЛОВ,<br/>
      НЕТВОРКИНГ С ЕДИНОМЫШЛЕННИКАМИ И ПРАКТИЧЕСКИЕ ЗАДАНИЯ<br/>
      ДЛЯ ФОТОГРАФОВ ЛЮБОГО УРОВНЯ ПОДГОТОВКИ.
    </p>

    <div class="program-cards">
      <!-- Карточка 1 -->
      <div class="program-card">
        <span class="program-number">1</span>
        <h3 class="program-card-title">Мастер-классы</h3>
        <p class="program-card-text">
          Учитесь у лучших фотографов города.<br/>
          Портретная, свадебная и коммерческая съёмка — разберём техники и секреты профессионалов на практических занятиях.
        </p>
      </div>

      <!-- Карточка 2 -->
      <div class="program-card">
        <span class="program-number">2</span>
        <h3 class="program-card-title">Нетворкинг</h3>
        <p class="program-card-text">
          Находите единомышленников, делитесь опытом и заводите полезные знакомства.<br/>
          Совместные проекты начинаются именно здесь, на PhotoMeet.
        </p>
      </div>

      <!-- Карточка 3 -->
      <div class="program-card">
        <span class="program-number">3</span>
        <h3 class="program-card-title">Фотопрогулки</h3>
        <p class="program-card-text">
          Применяйте полученные знания на практике.<br/>
          Групповые выездные съёмки по живописным местам Магнитогорска с разбором работ от менторов.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .program-section {
    padding-top: 140px; 
    padding-bottom: var(--spacing-xl);
    background: var(--background);
    position: relative;
    overflow: hidden;
  }

  /* Большой фоновый текст */
  .program-section::before {
    content: 'ЧТО ВАС ЖДЁТ';
    position: absolute;
    top: 60px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 180px;
    font-weight: 700;
    color: rgba(255, 255, 255, 0.03);
    white-space: nowrap;
    pointer-events: none;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
    position: relative;
    z-index: 1;
  }

  .program-label {
    text-align: center;
    font-size: var(--small-size);
    font-weight: 600;
    letter-spacing: 2px;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: var(--spacing-sm);
    font-family: var(--font-main);
  }

  .program-title {
    text-align: center;
    font-size: var(--h1-size);
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 var(--spacing-md) 0;
    font-family: var(--font-main);
    line-height: 1.1;
  }

  .program-description {
    text-align: center;
    font-size: var(--body-size);
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.6);
    max-width: 700px;
    margin: 0 auto var(--spacing-lg);
    font-family: var(--font-main);
  }

  .program-cards {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: var(--spacing-lg);
  }

  .program-card {
    position: relative;
    padding: 0 var(--spacing-lg);
  }

  /* Вертикальные линии между карточками */
  .program-card:not(:last-child)::after {
    content: '';
    position: absolute;
    right: 0;
    top: 60px;
    bottom: 0;
    width: 1px;
    background: rgba(255, 255, 255, 0.15);
  }

  .program-number {
    display: block;
    font-size: var(--h2-size);
    font-weight: 700;
    color: var(--primary);
    margin-bottom: var(--spacing-sm);
    font-family: var(--font-main);
  }

  .program-card-title {
    font-size: 22px;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 var(--spacing-sm) 0;
    font-family: var(--font-main);
  }

  .program-card-text {
    font-size: 15px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
    font-family: var(--font-main);
  }

  @media (max-width: 768px) {
    .program-section {
      padding-top: 60px;
      padding-bottom: 40px;
    }

    .program-section::before {
      font-size: 60px;
      top: 80px;
    }

    .program-title {
      font-size: 40px;
    }

    .program-description {
      font-size: var(--small-size);
    }

    .program-cards {
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .program-card {
      padding: 40px var(--spacing-md);
      text-align: center;
      border-bottom: 1px solid rgba(255, 255, 255, 0.15);
    }

    .program-card:last-child {
      border-bottom: none;
    }

    .program-card:not(:last-child)::after {
      display: none;
    }

    .program-number {
      text-align: center;
      margin-bottom: var(--spacing-sm);
    }

    .program-card-title {
      text-align: center;
      margin-bottom: 12px;
    }

    .program-card-text {
      text-align: center;
      font-size: var(--small-size);
    }

    .container {
      padding: 0 var(--spacing-md);
    }
  }
</style>
